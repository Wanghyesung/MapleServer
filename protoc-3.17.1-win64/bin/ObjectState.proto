syntax = "proto3";
package Protocol;

//repeated uint64 victims = 3;

//서버가 클라에게
message S_STATE
{
	uint32 scene_layer_id = 1;
	
	//텍스쳐
	string state = 2;
	int32 state_value = 3; //상위 8비트 방향, 하위 애님 인덱스
}

