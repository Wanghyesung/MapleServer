syntax = "proto3";
package Protocol;

//repeated uint64 victims = 3;

//서버가 클라에게
message S_STATE
{
	string scene = 1;
	uint32 layer_id = 2;
	
	//텍스쳐
	string state = 3;
	int32 anim = 4; //상위 8비트 방향, 하위 애님 인덱스
}

